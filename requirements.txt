Django==5.2.8
djangorestframework==3.15.2
django-cors-headers==4.4.0
# psycopg2-binary intentionally omitted for local dev (no prebuilt Windows
# wheel for newer Python versions). Add it back — or use psycopg[binary] —
# when deploying with a real Postgres/Neon database.
dj-database-url==2.2.0
python-decouple==3.8
gunicorn==22.0.0
whitenoise==6.7.0
requests==2.32.3
cloudinary==1.41.0
django-cloudinary-storage==0.3.0
Pillow==12.0.0